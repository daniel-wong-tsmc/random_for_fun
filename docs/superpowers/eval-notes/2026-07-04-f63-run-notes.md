# Eval run 2026-07-04 (F63) — rebaseline for the corroboration-doctrine prompt changes

Trigger: F63 amended all three SYSTEM prompts — extraction (F2e corroboration exception,
Task 3 bfd6571), thesis (anti-whipsaw >=3-publisher exception, Task 4 769a2d3), judge
(sufficiency rule sentence, Task 6 61b77f7). All three seam hashes drift; all 14 cases
need fresh brains + graders (no carry-over from the F62 run — every prompt changed).

Procedure: .claude/skills/run-eval/SKILL.md; all brains and graders inline tool-less Opus,
one per case, separate generations; F38 judge pairs get identical prompts as separate
generations. Run root: work/eval-f63-2026-07-04/ (worktree .worktrees/f63-corroboration,
branch f63-corroboration-doctrine).

Incumbents (F62 baseline): extract 6.75 / judge 7.50 / thesis 6.00; ties pass;
calibration negatives limit 4 each.

## Pre-committed disposition (recorded BEFORE any record-grade)

PASS -> `gpu-agent eval rebaseline --out work/eval-f63-2026-07-04 --as-of 2026-07-04
--reason "F63 corroboration doctrine: F2e exception + thesis anti-whipsaw exception +
judge sufficiency rule"`, commit baseline + RUN-NOTES, full suite (no deselects) green.
One FAIL -> diagnose per-case vs baseline, run ONE full replication.
Two FAILs -> STOP, keep the pin red, record BLOCKED-on-user with both runs' data.
NOT retry-until-green.

## In-run observation (extract-04, first generation)

The amended extraction sentence can tempt the brain to count publishers QUOTED INSIDE one
document as corroboration (extract-04 claimed high confidence citing "three distinct
publishers" for material all sourced from the single introl.com page). The gate counts
evidence URLs (which must be the document's own), so within-document corroboration can never
reach 3 — record-brain should reject these and the F38 re-dispatch fixes them. If this
recurs, consider a future prompt clarification ("across separately fetched documents") as
its own eval-gated change; noted for fix-backlog at handoff.

## Gate + grade-wave setup

record-brain attempt 1: 12/14 clean; TWO failures, both F38-safe re-dispatched:
- extract-04: the NEW F2e rule dropped two high-confidence drafts (1 distinct publisher < 3 —
  within-document corroboration correctly rejected); re-dispatch set both to medium with honest
  single-publisher basis. THE AMENDED GATE CAUGHT EXACTLY THE FAILURE MODE IT EXISTS FOR.
- judge-01: 'CEO' echo (4th occurrence across F62+F63 runs; the acronym is in the finding text
  itself, which is why brains keep echoing it — allowlist candidate noted for handoff).
Re-gate: 14/14, 0 failed. Judge-03-01 returned fenced JSON (fences stripped on save,
transport normalization). All four frozen-negative grade prompts verified byte-identical to
the F62 run (sha256) -> their grades (2/1/0/2, grader-reproduced three times) carry over.

## Attempt 1 verdict (record-grade --as-of 2026-07-04)

FAIL  seams: extract=6.38  judge=7.00  thesis=6.00 (tie, passes)
  - regression on 'extract': 6.375 < incumbent 6.750
  - regression on 'judge':   7.000 < incumbent 7.500

Per-case: extract 5,7,7,6,7,7,4,8 (01,02,03,03-01,03-02,04,05,06); judge 8,7,7,6
(01,02,03-01,03-02); thesis 7,5 (01,03-01).

## Attempt 1 diagnosis (per pre-committed disposition, before the ONE replication)

No deduction traces to the F63 prompt changes; every point lost is familiar
brain-coverage or grader-strictness variance:
- extract-01 (5): brain misattributed the 15%+ USG revenue-share to H200 (doc ties it to
  H20) and skipped Rubin ship-date + indirect-demand claims. Unrelated to the F63 sentence.
- extract-05 (4): completeness 0 for omitting the 11.8M-unit headline and attach-rate —
  the same case that scored lowest in F62 runs; coverage variance, unrelated to F63.
- extract-03-01 (6): typed the Samsung-2nm talks 'observed' where the curator wants
  'hypothesis'. Unrelated to F63.
- extract-04 (7): the ONLY case the F63 extraction sentence touches scored 7 with
  provenance-honesty 2 — the corrected single-publisher confidence basis was PRAISED.
- judge deductions were citation-group leakage (j03-01, j03-02 mechanism) and two grader
  errors on j03-02 (dinged 'Indonesian' though the finding says 'Batam, Indonesia'; dinged
  'optics' though the finding names Corning/Lumentum/Coherent). The NEW sufficiency
  sentence caused no deduction anywhere; j03-02 visibly KEPT the prior bottleneck under it.
- F62 precedent: identical-prompt reruns swung extract 6.25 -> 6.88 attempt-to-attempt.

Decision: ONE full replication (fresh brains + fresh graders, all 14 positive cases) at
work/eval-f63-2026-07-04-r2/. Frozen-negative grades carry only if r2 grade prompts are
again byte-identical. If r2 also fails: STOP, pin stays red, BLOCKED-on-user.

## Replication (r2) integrity note

First r2 dispatch of extract-04 accidentally included a non-canonical "precision note"
about the publisher-counting rule; that generation was DISCARDED unused and the case
re-dispatched with the canonical prompt verbatim. All other r2 dispatches are canonical.

## Replication (r2) gate waves

record-brain r2 attempt 1: ALL 8 extract answers failed schema on impact.direction — every
r2 generation wrote 'up'/'down'/'rising'/'falling' (or prose) instead of the enum
positive|negative|mixed. The canonical extraction prompt never states this enum (attempt-1
brains guessed right; r2 brains guessed wrong) — F38 re-dispatched all 8 with the verbatim
pydantic errors; all returned corrected enums with other fields unchanged. NOTED FOR
FIX-BACKLOG: the extraction prompt should state the impact.direction enum explicitly (its
own eval-gated prompt change).
record-brain r2 attempt 2: 13/14 clean; extract-04 (clean canonical generation) AGAIN claimed
high confidence from within-document "3 publishers" (The Register / Data Center Frontier
quoted inside the introl.com page) — F2e dropped it, second independent confirmation of the
systematic failure mode; F38 re-dispatched. Judge r2 generations avoided 'CEO' unprompted
("chief executive", "NVIDIA's chief").

## Replication (r2) verdict and FINAL DISPOSITION: BLOCKED-on-user

r2 record-grade --as-of 2026-07-04:
FAIL  seams: extract=6.38  judge=6.75  thesis=5.50
  - regression on 'extract': 6.375 < incumbent 6.750
  - regression on 'judge':   6.750 < incumbent 7.500
  - regression on 'thesis':  5.500 < incumbent 6.000
r2 per-case: extract 6,6,7,7,8,5,5,7; judge 7,7,7,6; thesis 6,5.

Both runs failed -> per the pre-committed disposition the run STOPS here: pin stays red,
no rebaseline, no --force, NOT retry-until-green. Both runs' complete data preserved at
work/eval-f63-2026-07-04/ (attempt 1) and work/eval-f63-2026-07-04-r2/ (replication).

## Cross-run analysis (for the user's decision)

1. Two independent full runs landed extract at EXACTLY 6.375; judge at 7.00 then 6.75;
   thesis at 6.00 then 5.50. The incumbent bar (6.75/7.50/6.00) is F62's attempt-3 —
   a high draw (F62's own attempts 1-2 scored extract 6.25 and 6.88 on IDENTICAL prompts).
   The bar appears to sit above the typical draw of the generation+grading process.
2. NO deduction in either run traces to the F63 prompt changes. Lost points are coverage
   variance (omitted secondary claims: 11.8M AIB units, attach rate, capital returns,
   Diversion Guidance, Maia/MTIA specifics) and grader strictness on trend/attribution
   nuances - the same failure modes F62's low attempts showed pre-F63.
3. The F63 behaviors themselves graded WELL where visible: extract-04's corrected
   single-publisher confidence basis was explicitly praised in both runs
   (provenance-honesty 2 in attempt 1); r2 judge-03-01 KEPT the prior binding constraint
   citing "that custom-silicon evidence rests on a single outlet" — the sufficiency rule
   working as designed and rewarded by the grader (crux 2, sensitivity 2).
4. The F2e gate caught the within-document-corroboration error in BOTH runs' extract-04
   generations — systematic model failure mode, exactly what the gate exists for.

## User options (merge remains blocked until one is chosen)

A. Judgment-call rebaseline with --force, on the diagnosis that both failures are
   incumbent-bar noise, not F63 regressions (fastest; explicitly a user-only call).
B. More replications to estimate the true mean (expensive; two runs already agree).
C. Land an eval-infrastructure change first (e.g., N-attempt mean or median as the
   baseline bar) as its own feature, then re-run F63's gate against it.
D. Hold F63 unmerged.

## Fix-backlog candidates surfaced by this run (each its own eval-gated change)

- Extraction prompt: state that corroboration counts publishers across separately
  fetched documents (2/2 fresh generations counted outlets quoted inside one document).
- Extraction prompt: state the impact.direction enum (positive|negative|mixed) —
  all 8 r2 generations guessed wrong ('up'/'rising'); attempt 1 guessed right by luck.
- acronyms.json: consider allowlisting 'CEO' (echoed from finding text 4x across runs).
- Eval infra: consider a multi-attempt bar (mean/median of N runs) to damp single-run
  baseline noise; F62 attempt spread (6.25-7.50 extract) exceeds the pass margin.
