# P3 — Contract v1.4 Migration Implementation Plan (F72 + F71, +F75 companion)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.

> **Status:** APPROVED to build (user signed off on all six §7 decisions 2026-07-06). Lane **P3**,
> worktree `.worktrees/contract-v1.4`, branch `fix/contract-v1.4`, build+review **Opus-class**. This
> is a **FROZEN-CORE Part-33 migration** — **only the user merges** (never AFK). Park at "awaiting
> user merge go" when done.

**Goal:** Ship contract v1.4 — make gate F2e's publisher-distinctness syndication-resistant (F72) and
resolve the anchor-bound vs sufficiency-gate deadlock via an exemption path (F71) — as one coupled
Part-33 migration, with F75's whole-run-bypass removal + trust-footer disclosure as companion doctrine
on the same branch.

**Architecture:** Two frozen-core gate-semantics changes (`gate.py` F2e counting; `sufficiency.py`/
`gate.py` anchor-forced exemption) + a data list (`registry/syndicators.json`) + companion non-frozen
work (`cli.py` flag removal, `brief.py` footer). Migration artifacts: a gate-behavior shadow-run (no
store writes), a migration doc with hand computation, charter reconciliation. **No scoring math moves**
(goldens unchanged) and **no emitted prompt bytes change** (the F6 pin must stay green).

**Tech Stack:** Python 3, pydantic, pytest.

## REQUIRED READING (before Task 1 — do not skip)
1. The design spec: `docs/superpowers/specs/2026-07-06-contract-v1.4-migration-design.md` — §1 rule
   changes, §3 schemaVersion (stays 1.2), §4 deliberately-unchanged, §5 shadow-run, §6 replay
   decision (none), §7 RESOLVED decisions, §10 acceptance gate, §11 fenced-off paths.
2. The runbook: `.claude/skills/gate-integrity-campaign/SKILL.md` — Phases 1 (F71), 2 (F75), 3 (F72),
   the solution menus, migration obligations, and the Fenced-off Wrong Paths table.
3. The mechanism traces + designed repro tests (authoritative for the exact test construction):
   - `.claude/skills/gate-integrity-campaign/references/f71-mechanism-and-repro.md` (§4 = the deadlock
     repro test design; §3 = the uncaught `JudgmentError`/`LLMError` crash path to close).
   - `.claude/skills/gate-integrity-campaign/references/f72-adversarial-fixture.md` (§2 = the
     constructed adversarial syndication fixture).
4. The Part-33 playbook: `.claude/skills/desk-change-control/references/part33-migration-playbook.md`
   (the "template for migration #3" checklist) and the two prior migration docs
   `docs/migrations/2026-07-contract-v1.{2,3}.md` for shape.

## Global Constraints (every task inherits these)
- **Lane isolation:** worktree `.worktrees/contract-v1.4`, branch `fix/contract-v1.4`. Never edit the
  root checkout's `main` or any other worktree.
- **FROZEN-CORE — only the user merges.** Do NOT merge, push, or switch branches. End parked on-branch.
- **schemaVersion stays 1.2** (§7 D3) — no `Finding` schema field added. F72's originating publisher
  is **gather-blob metadata**; F71's stamp rides the **existing `dimensionStatus` note** surface.
- **No emitted prompt bytes change** (§7 Q5 = leave extraction prompt unchanged) → `tests/test_evals_baseline_pin.py` MUST stay green at every step. If it trips, you changed a prompt — STOP; do not touch the pin or `fixtures/evals/baseline.json`.
- **No scoring math** — `fixtures/golden/*` unchanged; do not regenerate goldens.
- **Frozen-core files** (per roadmap:181 + `desk-change-control`): `gate.py`, `scoring.py`, Finding
  schema, `judgment/judge.py` aggregation, `pipeline.py`. `sufficiency.py` is added to this list in T5.
- **Fenced-off (do NOT do):** widen `_ANCHOR_TOL` (`gate.py:67`); delete/disable the sufficiency gate;
  hand-edit store/cycle-log/baseline; `git add -A`; re-gate/replay stored scorecards; SEC-EDGAR source
  ideas. (spec §11, campaign Fenced-off table.)
- **Python** `../../.venv/Scripts/python` from the worktree (shared root venv). Shell cwd resets — prefix
  every command with `cd /c/Users/danie/random_for_fun/.worktrees/contract-v1.4 &&`.
- **Commit discipline:** `git log --oneline -1` before every commit; stage only named files (never
  `-A`); commit messages via bash heredoc (no double quotes); end each with `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`. Use `feat(gate)!:` for the frozen-core rule commits (breaking-change marker, matching the v1.3 precedent).

---

### Task 0: Baseline
- [ ] `cd` into the worktree; run `../../.venv/Scripts/python -m pytest -q`. Confirm green (~1069/4 —
  P1/P2 are NOT in this branch; expect the pre-P1/P2 main count ~1066/4 since this branches from
  `08acf95`). Read all REQUIRED READING. Do not proceed on a red baseline.

### Task 1: F72(a) — syndicator registry + `collapsed_publisher`
**Files:** Create `registry/syndicators.json`; Modify `gpu_agent/publisher.py` (add `collapsed_publisher`); Test `tests/test_publisher.py`.
**Interfaces:** Produces `collapsed_publisher(evidence, *, bodies=None) -> str` beside `publisher_key`. Registry maps a syndicator netloc → its originating-publisher key; a non-syndicator netloc returns `publisher_key(evidence)` unchanged.
- [ ] Write failing test: 3 evidences on `stocktitan.net` / `markets.financialcontent.com` / `finance.yahoo.com` for one originating publisher collapse to 1 identity; 3 genuine outlets stay 3. (Fixture shape: `f72-adversarial-fixture.md` §2.)
- [ ] Run → fail (`collapsed_publisher` undefined). Implement the registry lookup + `registry/syndicators.json` seed (the three archetypal netlocs the backlog names, mapping to `originatingPublisher` — data only). Run → pass.
- [ ] Full suite + pin green. Commit `feat(gate)!: F72 syndicator registry + collapsed_publisher`.

### Task 2: F72(b) — L1 near-dup content collapse + wire into F2e count
**Files:** Modify `gpu_agent/publisher.py` (near-dup collapse) + `gpu_agent/gate.py:35` (F2e uses collapsed count); Test `tests/test_gate_corroboration.py`, `tests/test_publisher.py`.
**Interfaces:** F2e's `publishers = {publisher_key(e) for e in f.evidence}` becomes a collapsed set that also merges evidences whose bodies are exact `dedup.content_hash` matches (§7 Q6 = exact-hash now; normalized/shingle deferred). All three consumers inherit via the shared function.
- [ ] Write failing tests: (a) byte-identical bodies on non-registry domains → 1 distinct publisher → a finding fails the ≥3 bar; (b) 3 genuinely distinct outlets (different bodies, non-syndicator) → still pass at 3 (regression guard).
- [ ] Run → fail. Implement: extend `collapsed_publisher`/add a `distinct_publisher_count(evidence_list)` helper using `content_hash`; point `gate.py:35` at it. Keep the F2e error text `(K distinct publishers < N)`.
- [ ] Verify — not assume — all THREE consumers inherit: `gate.py` F2e, `thesis.py:477` rule 6, `wiki/lifecycle.py:59` promotion. Add/extend a test per consumer (or one shared param test) proving the collapse reaches each.
- [ ] Full suite + pin green. Commit `feat(gate)!: F72 L1 near-dup collapse in F2e distinct-count`.

### Task 3: F72 — originating-publisher gather-blob metadata (closes F69 note)
**Files:** the gatherer-contract/blob surface (NOT the Finding schema — D3). Modify the gather-blob metadata writer + its model; Test the relevant gather/blob test.
**Interfaces:** the gatherer records the originating publisher on blob metadata (structured), replacing free-text in `content`. schemaVersion stays 1.2 (no Finding field).
- [ ] Write failing test: a chased/corroborated blob carries an `originatingPublisher` metadata field. Run → fail. Implement on the blob-metadata surface only. Run → pass.
- [ ] Full suite + pin green. Commit `feat: F72 record originating publisher as gather-blob metadata`.

### Task 4: F71 — anchor-forced-move exemption + clean handler
**Files:** Modify `gpu_agent/sufficiency.py` (+ `gpu_agent/gate.py` where the anchor bound is computed); the stamp on the existing `dimensionStatus` note surface; the `JudgmentError`/`LLMError` handler (see `f71-mechanism-and-repro.md` §3). Test the deadlock scenario (design in `f71-mechanism-and-repro.md` §4).
**Interfaces:** an anchor-FORCED rating move (prior rating made illegal by the Part 7 bound) is exempt from the sufficiency gate and stamped `anchor-bounded on thin evidence` on the existing note; a genuine qualitative re-rate on the same thin evidence is STILL blocked.
- [ ] Write failing tests (per the reference §4 design): (a) anchor forces a move the sufficiency evidence can't support → resolves via exemption, no bypass, stamp present; (b) genuine re-rate, same thin evidence → still blocked (over-loosening guard); (c) the anchor-conflict path returns a clean re-dispatchable message under `--recorded`, not a traceback (reference §3).
- [ ] Run → fail. Implement the exemption + stamp (existing note surface, no schema bump) + the clean handler analogous to `_report_sufficiency_violations`. Run → pass.
- [ ] Full suite + pin green. Commit `feat(gate)!: F71 anchor-forced-move sufficiency exemption + clean handler`.

### Task 5: F75 companion — remove whole-run bypass, trust-footer disclosure, doctrine
**Files:** Modify `gpu_agent/cli.py` (remove whole-run `--no-sufficiency`; §7 Q3); `gpu_agent/brief.py::render_market_caveat` (brief.py:146 — add the conditional bypass/waiver line, sourced from the cycle log `gates.*`); the 2026-07-v3 retro note; `docs/roadmap.md:181` Standing Constraints (add `sufficiency.py` — §7 Q1); the machine-local `run-cycle` SKILL.md is OUT of the worktree — leave a TODO note in the final report (remove the "neither check ever blocks a scorecard" line). Test `tests/` brief-footer + cli.
- [ ] Write failing test: a cycle-log entry recording a bypass/waiver renders a waiver line in the brief trust footer; a clean cycle renders none. Run → fail. Implement the conditional footer line + remove the whole-run flag (F71's exemption removes its only sanctioned use). Run → pass.
- [ ] Add `sufficiency.py` to roadmap:181 Standing Constraints (frozen-core list) — §7 Q1.
- [ ] `Select-String`/grep check: no whole-run `--no-sufficiency` on live paths (`gpu_agent/cli.py`).
- [ ] Full suite + pin green. Commit `feat: F75 remove whole-run sufficiency bypass; trust-footer waiver disclosure`.

### Task 6: Migration artifacts + shadow-run
**Files:** Create `scripts/shadow_run_v14.py` (NO store writes); Create `docs/migrations/2026-07-contract-v1.4.md`; Modify `docs/agent-swarm-charter.md` (Part 33 register migration #3; reconcile Part 37 + any deferred-list clause in the SAME change — do not leave it self-contradictory).
- [ ] Build `scripts/shadow_run_v14.py`: over every stored finding in `store/chips.merchant-gpu/*.json`, compute F2e distinct-count old (raw netloc) vs new (collapsed) and the F71 precedence outcome old vs new; emit a flip table. No store writes. (spec §5.)
- [ ] Write `docs/migrations/2026-07-contract-v1.4.md` per the Part-33 template (spec §8): header + this plan's approval citation; rule changes one line per F-id; "the silent hole F72 closes"; schemaVersion stays 1.2; "Deliberately unchanged" (goldens/scoring/schema); the shadow-run flip table + honesty section (did any stored high-confidence finding/thesis reversal/wiki promotion RELY on the hole? if yes → retro trust-footer note per §6, NOT a replay); "Replay: none (v1.3 precedent)"; "Companion doctrine (F75 + sufficiency.py waiver + charter reconciliation)".
- [ ] Put an **independent hand computation** of at least one changed distinct-count in the migration commit message (write out the count old vs new by hand; close with "matches the code").
- [ ] Charter: register migration #3 (Part 33) and reconcile Part 37 corroboration doctrine + deferred list in the same edit.
- [ ] FINAL: full suite green; `tests/test_evals_baseline_pin.py` green (confirm, don't assume); goldens unchanged (`git diff 08acf95..HEAD -- fixtures/golden` empty). Commit `docs(migration): contract v1.4 migration doc + shadow-run + charter reconciliation`.

---

## Acceptance gate (from spec §10 — measurable, never by eye)
- [ ] Syndicated story on 3 domains → fails ≥3 bar; 3 genuine outlets → still pass (T1/T2 tests).
- [ ] All three `publisher_key` consumers verified to inherit the collapse (T2).
- [ ] Anchor-forced deadlock resolves via exemption, no `--no-sufficiency`; genuine thin re-rate still
  blocked; no anchor-conflict traceback (T4).
- [ ] No whole-run `--no-sufficiency` on live paths; bypass/waiver renders in the footer (T5).
- [ ] Full suite green; eval pin green; goldens unchanged; shadow-run flip table committed (T6).
- [ ] Opus-class review on the frozen diff; branch parked at "awaiting user merge go".

## On completion
Write `.superpowers/handoffs/contract-v1.4-DONE.md` (on disk; do NOT force-commit — `.superpowers/` is
gitignored, matching P1/P2). Report: per-task commits, suite/pin/goldens status, the shadow-run
honesty finding (did anything historical rely on the hole?), the machine-local `run-cycle` SKILL.md
TODO (F75), and confirmation you did NOT merge/push. **Then STOP — the user reviews and merges.**
