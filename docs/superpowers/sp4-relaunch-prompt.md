You're picking up an in-flight project in C:\Users\danie\random_for_fun (a Python "GPU Category Agent" that runs *inside* Claude Code — Claude Code itself is the brain: no OAuth token, no SDK, no `[llm]` extra, no external API). Start by reading `docs/superpowers/HANDOFF.md` — it's the resume doc and points to everything else.

The in-flight task: continue **sub-project 4 — "the daily demand/supply monitor"** (umbrella spec `docs/superpowers/specs/2026-06-27-daily-monitor-decomposition-design.md`). **4-1, 4-2, 4-3, 4-4a, and 4-4b are DONE + merged** to local `main` (at `8cee8a3`; suite **332 passed, 3 skipped**; frozen core byte-unchanged). The 4-4 sequence was **REORDERED** this session: discovery (4-4c) needs gathered raw material to notice off-list topics, and the gather firehose is 4-4d — so **4-4d is now built BEFORE 4-4c**. New order: **4-1 → 4-2 → 4-3 → 4-4a → 4-4b → 4-4d → 4-4c → 4-5.** Sub-project **4-4d's spec + plan are written and committed on `main`** (`3b1f216` spec, `fad9203` plan); **not yet built**; nothing pushed since the 4-1/4-2/4-3 baseline (`origin/main` == `aabc4c8`; local `main` is 18 commits ahead — push only when the user asks). Working tree clean.

Do this:
1. Read `HANDOFF.md`, then the **4-4d plan** `docs/superpowers/plans/2026-07-01-daily-gather-scrape-dedup.md` and its **spec** `docs/superpowers/specs/2026-07-01-daily-gather-scrape-dedup-design.md` (§0 has the locked decisions + the reorder; §10 the 4-4d↔4-4c seam). Skim the existing Part-37 ingest seam `gpu_agent/gathering/ingest.py` (`normalize_documents` — 4-4d's L1 runs AFTER it, frozen); the merged **4-4a/4-4b** (`2026-06-29-wiki-ingest*`, `2026-06-30-wiki-lint*` — 4-4d feeds 4-4a `wiki-ingest` and its UPDATEs become 4-4b material moves); and the **4-2** `sourceInventory` + `cadenceHorizon` (scrape sources + cadence prioritization). Check the SDD ledger `.superpowers/sdd/progress.md` and `git log` so you don't redo finished work.

2. Execute the 4-4d plan via **superpowers:subagent-driven-development**: fresh **sonnet** implementer per task, two-stage **sonnet** review between tasks (spec + quality), **opus final whole-branch review**, on a branch `sp4-4d-…` off `main`. The plan is **6 TDD tasks** ending at **357 passed, 3 skipped**: (1) dedup data models + `DedupConfig`; (2) `SeenDocIndex` + L1 pre-brain document dedup (URL + content-hash); (3) prior-vintage lookup (via the entity page's observations — `FindingStore` has no iteration) + the tolerance-based change test; (4) L2 `classify_findings` — NEW/UPDATE/DUPLICATE + intra-batch collapse; (5) CLI wiring — `ingest --dedup-store` (L1) + a new `wiki-dedup` subcommand (L2) + frozen guard; (6) daily gather mode + numeric scrape skill edits (`gather-category`/`run-cycle`) — **SESSION-RUN, dry-run validated, no pytest** (like sp2 Task 3 / sp3-A Task 10). Keep the ledger updated (append the per-task lines to the `sp4-4d` section). Merge to `main` (local fast-forward, like 4-1/4-2/4-3/4-4a/4-4b). Then a **throwaway preview-render** (seed a store; run `ingest --dedup-store` twice to show L1 drop known docs; then `wiki-dedup` to show the NEW/UPDATE/DUPLICATE split + the `DedupReport` going live).

3. Then continue the sequence: **4-4c** (discovery lane on live gather input) → **4-5** (per-category Market-State brief render), each its own brainstorm → spec → **user-review gate** → writing-plans → SDD → merge.

4-4d is the daily **INPUT FIREHOSE** (Part 37/22/29/17/20). What it builds (acceptance = spec §13): a pure-code **cross-run dedup-vs-store** engine (`gpu_agent/gathering/dedup.py`) — **L1** a persistent `SeenDocIndex` (normalized URL + content-hash → first-seen `asOf`) that drops already-seen documents **before** extraction; **L2** a finding-level classifier that, per `(entity, indicatorId)` vs the store's latest vintage, splits gated findings into **NEW / UPDATE / DUPLICATE** (NEW+UPDATE ingest via 4-4a; DUPLICATE logged + dropped), tolerance-based (1% default) with an intra-batch collapse; a `DedupReport` counts everything (**nothing silent**). Wired additively into the CLI (`ingest --dedup-store` L1; new `wiki-dedup` subcommand L2). Plus the **daily gather mode** (recency-windowed Part-37 loop, cadence-prioritized, caps logged) + the **numeric scrape sweep** — a permissive daily source (e.g. `gpuSpotPrice`) is a gatherer snapshot → the **FROZEN** `extract → gate` → a measured Finding (no code number-path; **Part 22**: paywalled labeled `estimate`, never fetched).

The 4 locked 4-4d design decisions (from this session's brainstorm — don't relitigate): (a) **reorder** 4-4d before 4-4c; (b) **full firehose** (all three pieces in one sub-project); (c) **hybrid dedup** (L1 doc URL/hash pre-brain + L2 finding-level NEW/UPDATE/DUPLICATE post-gate); (d) numeric scrape rides the **FROZEN** brain (gatherer-snapshot → extract→gate, no new code number-source path). Plus: L1 memory = a new persistent seen-doc index; L2 "changed" = tolerance-based; one 4-4d spec (code core via SDD/pytest + skill edits via dry-run) on one branch.

Plan realizations pinned (the plan is the authority): the module is `gpu_agent/gathering/dedup.py` (the **EXISTING** `gathering/` package, not a new `gather/`); `DedupReport` carries the finding-level verdicts while L1's doc drops fold into the `ingest` gather-log (`droppedKnown`) — the two run at different pipeline stages; the current `ingest` subparser has only `--blobs`/`--out`/`--primary-sources`, so Task 5 adds `--dedup-store` **AND** `--as-of`.

Honor the invariants in the HANDOFF "OPERATING NOTES / INVARIANTS": run from repo root with `.venv/Scripts/python`; **truly frozen — byte-unchanged**: `gate.py`, `scoring.py`, `registry/indicators.py`+`validate.py`, the Finding schema, the 6 dimension names, the rating scale, `pipeline.py` Part-7 gate, `JsonStore`/`FindingStore`, every existing member of `gpu_agent/wiki/store.py`+`log.py`+`page.py`+`ingest.py`+`lint.py`, **AND** the existing Part-37 seam `gpu_agent/gathering/ingest.py` (`normalize_documents` — L1 runs after it, does not modify it); **additive only** (Part 33): the new `gpu_agent/gathering/dedup.py` + the additive `cli.py` edits (`ingest --dedup-store` + `wiki-dedup`) + the gitignored runtime `seen_docs.jsonl` + the `gather-category`/`run-cycle` skill edits. Doctrine: numbers only from gated findings (Part 17 — the dedup reads structured `Finding`/`RawDocument` values, writes none; the scrape produces no number outside the frozen gate); replayable (Part 20 — the seen-doc index + snapshot + `DedupReport`); nothing silent (Part 29 — every dropped-known doc + every DUPLICATE finding is counted and listed); the dedup mutates no stored finding or page (it classifies and routes; NEW/UPDATE ingest goes through the existing 4-4a idempotent writer). Full suite stays green after every task (332/3 → 357/3). Every commit ends with `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`. Push only when the user asks.
