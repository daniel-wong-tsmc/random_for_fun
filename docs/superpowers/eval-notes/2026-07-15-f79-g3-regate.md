# F79 scoring-v2 — G3 bundled eval re-gate run notes (2026-07-15)

**Authorization:** user-authorized, interactive, 2026-07-15 (relayed via orchestrator).
**Branch:** f79-scoring-v2. **Run dirs (gitignored `work/`):** `work/eval-2026-07-15/{r1,r2,r3}`.

## What was gated

Stage 7 promoted the six SDEWS series indicators into `registry/indicators.json`
(pkgCapacityOrderSpread S1, hbmSupplyCapex S2, hyperscalerCapexRevision D1,
odmMonthlyAiRevenue D9, tokenEconomics D4, marginalBuyerFinancing X5). This added six
vocabulary lines to the **extract** system prompt, moving only the extract seam's prompt
hash and turning the F6 pin RED by design. This run re-gated that prompt delta.

## Method

run-eval skill, eval-v2 (3 replicates). Each positive case dispatched to a separate
tool-less Opus subagent (brains), graded by separate tool-less Opus rubric judges;
answers saved byte-verbatim and passed through `record-brain` / `record-grade`
(as-of 2026-07-15). Gate-rejected cases were re-dispatched individually with the printed
violations appended (never hand-edited): a handful of judge/grader answers were
re-dispatched for JSON-shape issues (leading prose, an extra `score_note`/`verdict` key,
one unclosed brace) — all schema, not scoring, defects.

Dispatch-mechanics note (disclosed): r1 extract brains were dispatched with the prompt
inlined; all other brains/graders (r1 non-extract, r2, r3, and every grader) were
dispatched by having each tool-less Opus subagent Read its own machine-generated,
byte-exact prompt file and use no other tool. This preserves generation purity (one fixed
input, no research/browse) while guaranteeing byte-exact prompt delivery and containing
the token spend. The emitted prompt bytes are identical across r1/r2/r3 (verified).

## Verdict (seam-scoped, F65g)

| seam | r1 | r2 | r3 | binds? |
|---|---|---|---|---|
| extract | 6.875 | 6.625 | 6.500 | YES (prompt hash moved) |
| judge | 7.75 | 7.25 | 7.00 | no — hash-identical, informational |
| thesis | 6.00 | 6.00 | 6.00 | no — hash-identical, informational |
| implication | 8.00 | 8.00 | 8.00 | no — hash-identical, informational |

- r1 alone: **marginal-pass** on extract (6.875 in the band [bar 6.75, mean 7.0)).
- Per the F73 symmetric-band rule, replicated once (r2). `eval verdict --runs r1 r2`:
  **PASS** — extract two-run mean **6.750 = bar 6.750** (bar-touch passes); only extract
  gated, the other three informational. Proof: `work/eval-2026-07-15/r2/verdict.json`.
- r3 is the unfiltered top-up (kept regardless of score; it came in marginal-fail
  standalone with a crater on extract-04, which is legitimate replicate noise, not a
  discardable draw). Extract replicate range 0.375 < dispersion guard 1.0.

## Rebaseline

`eval rebaseline --runs r1 r2 r3 --verdict r2/verdict.json` (governance: PASS verdict +
matching prompt hashes). New `fixtures/evals/baseline.json`: extract prompt hash re-pinned
to the promoted `88b6f4f0…`; extract seamMean 6.667, eps 0.382 (bar 6.285); judge 7.333,
thesis 6.0, implication 8.0. **F6 pin now GREEN** (`test_prompt_hashes_match_baseline`
and `test_baseline_integrity` both pass). Full suite: 1618 passed, 7 skipped.

## F73 canary live-capture (folded into this run)

Damage (working-tree only, restored after; the doc-prescribed method): the six new series
ids stripped from `registry/indicators.json`, so the extract system prompt loses those six
vocabulary lines (damaged extract hash `c8cf1808…`, i.e. the pre-F79 vocab, != the new
baseline `88b6f4f0…` -> extract binds). One live run: eight damaged Opus extract brains +
eight Opus graders; the eleven hash-identical non-extract seams reused r3's answers/grades
(their prompt bytes are byte-identical under the strip, verified). A context-free brain
mis-mapped the stripped signals (e.g. hyperscaler-capex and TSMC-3nm to rpoBacklog/S10 in
extract-04; the financing model to D2 in extract-03-02), which the graders penalized.

Result — captured to `fixtures/evals/canary/extract-series-vocab-stripped/report.json`:
**decision marginal-fail, pass=False; extract gated (binds) at 6.25 < bar 6.285; the one
reason names extract.** `tests/test_evals_canary_f79.py` is now active (its
`skipif(not CANARY.exists())` auto-enables on the captured fixture) and PASSES — standing
proof the F79 prompt change has teeth. Registry restored; F6 pin green after restore. (The
margin is thin: 6.25 vs 6.285. The strip only damages the two cases whose documents carry
the new series signals, so the seam-mean hit is modest; it still crosses the bar, but a
future baseline shift could narrow it — worth a note if the canary is ever re-captured.)

## Remaining after G3

G3 core is complete: rebaseline done, F6 pin green, canary captured + test passing. Next
stage: shadow soak (>=5 live cycles) -> G4 cutover (its own user gate). This eval run does
NOT write the DONE sentinel.
