You're picking up an in-flight project in C:\Users\danie\random_for_fun (a Python "GPU Category Agent" that runs *inside* Claude Code — Claude Code itself is the brain: no OAuth token, no SDK, no `[llm]` extra, no external API). Start by reading `docs/superpowers/HANDOFF.md` — it's the resume doc and points to everything else.

The in-flight task: continue **sub-project 4 — "the daily demand/supply monitor"** (umbrella spec `docs/superpowers/specs/2026-06-27-daily-monitor-decomposition-design.md`). **4-1, 4-2, 4-3 are DONE + merged + pushed** (`main` == `origin/main` at `92434a0`; suite **282 passed, 3 skipped**; frozen contract byte-unchanged vs `559abd0`). Sub-project **4-4** (the daily gather + relevance + discovery engine) has been **decomposed into 4-4a → 4-4b → 4-4c → 4-4d** (see the 4-4a spec §0). **4-4a's spec + plan are written and committed on `main`** (`f27c944` spec, `16db7cd` plan); **not yet built**; not pushed since 4-3. Working tree clean.

Do this:
1. Read `HANDOFF.md`, then the **4-4a plan** `docs/superpowers/plans/2026-06-29-wiki-ingest.md` and its **spec** `docs/superpowers/specs/2026-06-29-wiki-ingest-design.md` (its §0 has the full 4-4 decomposition). Skim the merged **4-1** spec/plan (`2026-06-27-temporal-store-wiki*`) — 4-4a writes into that wiki store — and the merged **4-2/4-3** plans as the SDD pattern. Check the SDD ledger `.superpowers/sdd/progress.md` and `git log` so you don't redo finished work.
2. **Execute the 4-4a plan** via **`superpowers:subagent-driven-development`**: fresh **sonnet** implementer per task, two-stage **sonnet** review between tasks (spec + quality), **opus final whole-branch review**, on a branch `sp4-4a-…` off `main`. The plan is **4 TDD tasks** ending at **300 passed, 3 skipped**:
   - Task 1 — `WikiStore.set_body` (additive, idempotent body writer).
   - Task 2 — `gpu_agent/wiki/ingest.py`: `slug` + `IngestResult`/`PageEnrichment` + `INGEST_SYSTEM` + Phase-1 `route_findings` (deterministic entity routing) + `build_bundle` (emit).
   - Task 3 — `apply_enrichment` (Phase-2 applier: idempotent `set_body`/`record_state`/crossRefs; one `ingest` log event; fail loud on missing/non-entity page).
   - Task 4 — `wiki-ingest` CLI subcommand (Phase-1-always + `--emit-prompt`/`--recorded` seam) + a committed `fixtures/recorded/ingest-merchant-gpu.json` integration test + frozen/fixtures `git diff` guards.
   Keep the ledger updated (append a `sp4-4a` section; mark each task complete when its review is clean). **Merge to `main`** (local fast-forward, like 4-1/4-2/4-3). Then a **throwaway preview-render** (`wiki-ingest --emit-prompt` on `fixtures/golden/findings.json`, plus a `--recorded` apply) to show entity pages going live.
3. Then continue the sequence: **4-4b → 4-4c → 4-4d**, each its own brainstorm → spec → user-review gate → writing-plans → SDD → merge (the established pattern). Then **4-5** (the per-category Market-State brief render).

What 4-4a is (acceptance = spec §9): a `wiki-ingest` CLI that (Phase 1, code) routes each gated finding to `entity:<slug(finding.entity)>` idempotently (auto-create provisional pages, append observations, fail loud on empty entity), then (Phase 2, brain via `--emit-prompt`→`--recorded`) enriches **existing entity pages only** — prose body, state/trajectory/salience, crossRefs, contradiction notes, one `ingest` log event per run. Adds the additive `WikiStore.set_body`; everything else reuses the 4-1 store API. **Themes, the explore budget, provisional off-registry discovery, quarantine, and promotion are 4-4c — NOT 4-4a.** Materiality scoring + salience decay are 4-4b. Daily gather mode + scrape sweep + dedup-vs-store are 4-4d.

Honor the invariants (HANDOFF "OPERATING NOTES / INVARIANTS"): run from repo root with `.venv/Scripts/python`; **Claude Code is the brain** (no token/SDK/`[llm]`/API; the ingest brain step is the `--emit-prompt`→`--recorded` seam answered by a dispatched Opus subagent); **truly frozen — byte-unchanged:** `gate.py`, `scoring.py` (incl. `dmi_smi_contribution`), `registry/indicators.py` + `validate.py`, the `Finding` schema, the 6 dimension names, the rating scale, `pipeline.py` Part-7 gate, the existing `JsonStore`/`FindingStore`, and **every existing member of `gpu_agent/wiki/{store,log,page}.py`** (4-4a only *adds* `WikiStore.set_body`); **additive only (Part 33):** the new `wiki/ingest.py`, the `wiki-ingest` CLI subcommand, new fixtures. Doctrine: every finding is gated through `FindingStore` before it lands (numbers only from gated findings, Part 17); the brain curates prose/state but invents no measured values; the `ingest` log event + idempotent re-runs keep every day replayable (Part 20); caps/skips/unroutable findings/missing-page ops are **logged or fail loud, never silent** (Part 29). Full suite stays green after every task (baseline **282 passed, 3 skipped** → **300/3** at the end of 4-4a). Every commit ends with `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`. Push only when the user asks.

Bigger picture (don't lose it): the north star is a **daily, explainable GPU-market intelligence monitor running entirely inside Claude Code, one interface, Claude Code as the brain.** 4-1 built the temporal store; 4-2 added daily/leading indicators; 4-3 added the Momentum-vs-Outlook indices + divergence (Outlook reads `insufficient-coverage` until 4-4 feeds leading findings). **4-4 is what makes the store, the threads, and Outlook finally *live*** — and 4-4a (brain ingest) is its keystone writer. The *real* "understand the whole GPU market" product is the **layer-tier synthesis** that comes after the per-category mechanics (4-5) are proven.
