You're picking up an in-flight project in C:\Users\danie\random_for_fun (a Python "GPU Category Agent" that runs *inside* Claude Code — Claude Code itself is the brain: no OAuth token, no SDK, no `[llm]` extra, no external API). Start by reading `docs/superpowers/HANDOFF.md` — it's the resume doc and points to everything else.

The in-flight task: continue **sub-project 4 — "the daily demand/supply monitor"** (umbrella spec `docs/superpowers/specs/2026-06-27-daily-monitor-decomposition-design.md`). **4-1, 4-2, 4-3, and 4-4a are DONE + merged** to local `main` (at `6c41e14`; suite **300 passed, 3 skipped**; frozen core byte-unchanged through 4-4a). Sub-project **4-4** (the daily gather + relevance + discovery engine) is decomposed into **4-4a → 4-4b → 4-4c → 4-4d** (see the 4-4a spec §0). **4-4b's spec + plan are written and committed on `main`** (`e020a43` spec, `6c41e14` plan); **not yet built**; nothing pushed since the 4-1/4-2/4-3 baseline (`origin/main` == `aabc4c8`; local `main` is 7 commits ahead — push only when the user asks). Working tree clean.

Do this:
1. Read `HANDOFF.md`, then the **4-4b plan** `docs/superpowers/plans/2026-06-30-wiki-lint-relevance-engine.md` and its **spec** `docs/superpowers/specs/2026-06-30-wiki-lint-relevance-engine-design.md` (the §0 + §11 lock the 4-4b↔4-4c boundary). Skim the merged **4-4a** spec/plan (`2026-06-29-wiki-ingest*`) — 4-4b reads what 4-4a wrote (the `ingest`/contradiction signal + the daily `diff`) and reuses `WikiStore.set_body` — and the **4-2** `cadenceHorizon` tags via `gpu_agent/registry/horizon.py` (`IndicatorHorizons`), which 4-4b's decay half-life reads. Check the SDD ledger `.superpowers/sdd/progress.md` and `git log` so you don't redo finished work.
2. **Execute the 4-4b plan** via **`superpowers:subagent-driven-development`**: fresh **sonnet** implementer per task, two-stage **sonnet** review between tasks (spec + quality), **opus final whole-branch review**, on a branch `sp4-4b-…` off `main`. The plan is **6 TDD tasks** ending at **332 passed, 3 skipped**:
   - Task 1 — `gpu_agent/wiki/ingest.py`: contradiction `format_contradiction_detail`/`parse_contradiction_detail` seam + behavior-preserving `apply_enrichment` detail refactor + the folded `PageEnrichment.salience` `[0,1]` bound.
   - Task 2 — `gpu_agent/wiki/lint.py`: data models (`MaterialMove`/`HealthReport`/`LintReport`/…) + the tunable `LintConfig` policy table.
   - Task 3 — salience **decay**: tag-derived `half_life` (cadence-driven, leading-floored, longest-class-wins, untagged→logged) + cycle-count `quiet_age` + non-destructive `effective_salience`.
   - Task 4 — the **materiality scorer** (`_score_move`/`score_moves`): the 4 factors, hybrid weighting, tier/recency/leading multipliers, threshold → material/dropped split.
   - Task 5 — **structural health** (`health_report`): orphans, stale, asymmetric + mention-without-link cross-ref gaps, contradiction roll-up.
   - Task 6 — `lint()` assembly + `wiki-lint` CLI subcommand + one idempotent `lint` log event per `asOf` + frozen/fixtures `git diff` guards.
   Keep the ledger updated (append an `sp4-4b` section; mark each task complete when its review is clean). **Merge to `main`** (local fast-forward, like 4-1/4-2/4-3/4-4a). Then a **throwaway preview-render** (seed a store via `wiki-ingest` on `fixtures/golden/findings.json`, then `wiki-lint --as-of …` to show the ranked `LintReport` — material moves, decayed `effective_salience`, the health report — going live).
3. Then continue the sequence: **4-4c → 4-4d**, each its own brainstorm → spec → user-review gate → writing-plans → SDD → merge (the established pattern). Then **4-5** (the per-category Market-State brief render).

What 4-4b is (acceptance = spec §14): a **pure-code** `wiki-lint` pass (`gpu_agent/wiki/lint.py` + CLI, **no new brain step** — the brain's one semantic judgment, "contradicts the thesis?", already happened at 4-4a ingest) that ranks the daily `diff`'s **material moves** (new-thread | state/trajectory change | contradicts-thesis[highest] | moves-scoring-indicator∝magnitude, × tier × recency × leading-boost × the brain's intrinsic `salience`; below-threshold moves **dropped + logged**), applies **non-destructive salience decay** (per-thread half-life **input-derived** from 4-2's `cadence × horizon` tags; `quiet_age` by cycle-count; `effective_salience = intrinsic × 0.5^(quiet_age/half_life)`; a `stale` flag), and a **structural health** pass (orphans / stale / asymmetric + mention-without-link cross-ref gaps / contradiction roll-up). The contradiction is read via a **shared `format`/`parse` helper in `ingest.py`** (`LogEvent` is frozen, so the helper is the seam). Emits one **idempotent `lint` log event** per `asOf`. **The 4-4b↔4-4c boundary is locked (spec §11):** 4-4b produces *signals*; 4-4c (discovery — explore budget, theme pages, provisional/quarantine/promotion) *consumes* them (materiality → promotion; `stale` → provisional pruning). **Theme pages, the explore budget, provisional discovery, quarantine, and promotion/pruning actions are 4-4c — NOT 4-4b.** The daily gather + scrape sweep + dedup-vs-store are 4-4d. The brief render is 4-5. **Pure-semantic (no-mention) entity↔entity cross-ref suggestions are a deferred follow-up** (a future optional lint brain-seam), logged in the spec.

Honor the invariants (HANDOFF "OPERATING NOTES / INVARIANTS"): run from repo root with `.venv/Scripts/python`; **Claude Code is the brain** (no token/SDK/`[llm]`/API; 4-4b adds NO brain step — it's deterministic code over what the brain already curated); **truly frozen — byte-unchanged:** `gate.py`, `scoring.py` (incl. `dmi_smi_contribution`), `registry/indicators.py` + `validate.py`, the `Finding` schema, the 6 dimension names, the rating scale, `pipeline.py` Part-7 gate, the existing `JsonStore`/`FindingStore`, and **every existing member of `gpu_agent/wiki/store.py` / `log.py` / `page.py`** (incl. 4-4a's `set_body`); **modified additively/behavior-preserving:** `gpu_agent/wiki/ingest.py` (the contradiction helpers + the `apply_enrichment` detail refactor — output string unchanged, existing 4-4a tests stay green — + the `PageEnrichment.salience` bound); **additive only (Part 33):** the new `wiki/lint.py`, the `wiki-lint` CLI subcommand. Doctrine: every finding is gated through `FindingStore` before it lands (numbers only from gated findings, Part 17); 4-4b sets **no** number on a page/index uncomputed and **mutates no page lifecycle** (no `record_state`/promote/prune — that's 4-4c); the brain invents no measured values; the decay is **read-time / non-destructive** (the brain-set `salience` is never overwritten) and the `lint` log event keeps every day replayable (Part 20); below-threshold/dropped moves, untagged-indicator half-life fallbacks, and health gaps are **logged or reported, never silent** (Part 29). Full suite stays green after every task (baseline **300 passed, 3 skipped** → **332/3** at the end of 4-4b). Every commit ends with `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`. Push only when the user asks.

Bigger picture (don't lose it): the north star is a **daily, explainable GPU-market intelligence monitor running entirely inside Claude Code, one interface, Claude Code as the brain.** 4-1 built the temporal store; 4-2 added daily/leading indicators; 4-3 added the Momentum-vs-Outlook indices + divergence; 4-4a made the store/threads/Outlook **live** (the brain ingest writer). **4-4b is the early-warning *engine*** — it tells you *what actually moved and matters today* (materiality), *what's gone quiet* (decay), and *where the wiki is unhealthy* (lint), turning the raw daily `diff` into a ranked, explainable signal that 4-5's brief renders and 4-4c's discovery lane consumes. The *real* "understand the whole GPU market" product is the **layer-tier synthesis** that comes after the per-category mechanics (4-5) are proven.
