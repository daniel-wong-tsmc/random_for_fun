You're picking up an in-flight project in C:\Users\danie\random_for_fun (a Python "GPU Category Agent" that runs *inside* Claude Code — Claude Code itself is the brain: no OAuth token, no SDK, no `[llm]` extra, no external API). Start by reading `docs/superpowers/HANDOFF.md` — it's the resume doc and points to everything else.

The in-flight task: continue **sub-project 4 — "the daily demand/supply monitor"** (umbrella spec `docs/superpowers/specs/2026-06-27-daily-monitor-decomposition-design.md`). Sub-projects 1–3 are DONE+merged+pushed. Within sp4: **4-1 (temporal store + LLM-wiki) is DONE and merged to `main` (`3a0a9c5`).** **4-2's spec is already written and committed** (`96af2c3`). **Everything is pushed — `origin/main` == local `main` (all sp4 docs through the 4-2 spec + this handoff are on GitHub).** Suite: **248 passed, 3 skipped.** Working tree clean; frozen contract intact. (Going forward, push only when the user asks.)

Do this:
1. Read `HANDOFF.md`, then the **4-2 spec** `docs/superpowers/specs/2026-06-29-leading-daily-indicators-design.md`, then the **human-output design target** `docs/superpowers/specs/2026-06-29-human-market-brief-design-target.md` (esp. its §0 scope correction), then charter **Part 39** (`docs/agent-swarm-charter.md`) and the taxonomy `docs/taxonomy.json` (the 5-layer cake, ~33 leaf-category agents). Skim the merged 4-1 spec/plan for the established pattern.
2. **Finish 4-2** (spec already written): take it through the **user-review gate**, then `superpowers:writing-plans` → `docs/superpowers/plans/2026-06-29-leading-daily-indicators.md` (TDD, bite-sized; the merged 4-1 plan is the template) → `superpowers:subagent-driven-development` (fresh subagent per task, two-stage review between tasks with sonnet implementers/reviewers, **opus final whole-branch review**), on a branch `sp4-2-…` off `main`, **merged to `main`** (local fast-forward, like 4-1). Keep the ledger `.superpowers/sdd/progress.md`. Then **preview-render** merchant-gpu's own-lane brief with the new tags (throwaway, to show progress).
   - 4-2 builds (acceptance = spec §10): 5 in-lane indicators (`rpoBacklog`, `vendorRevenueGuidance`, `leadTimes` scoring; `designWins` structural + `gpuSpotPrice` price as overlays) in `registry/indicators.json`; a top-level **`cadenceHorizon`** map tagging **all 17** indicators (cadence × horizon, the C-3 top-level-map pattern); a new accessor `gpu_agent/registry/horizon.py` (`IndicatorHorizons` + a `validate_coverage` that fails loud if any *scoring* indicator is untagged) + tests; source-inventory + `manifests/chips.merchant-gpu.json` coverage for the 5 new (reuse C). DMI/SMI on committed fixtures must stay unchanged (new indicators are inert until 4-4 feeds them); frozen contract byte-unchanged; full suite green.
3. Then continue the sequence: **4-3 → 4-4 → 4-5** (each its own brainstorm → spec → writing-plans → SDD → merge, the sp3/4-1 pattern). 4-3 = the two indices (Momentum lagging+coincident vs Outlook leading, split demand/supply, each with SDGI; computed in code; reads the `cadenceHorizon` tags via `registry/horizon.py`; surface the Momentum-strong-while-Outlook-turns divergence). 4-4 = daily gather + scrape cron + materiality/`lint` + brain ingest + **the discovery engine** (provisional threads for topics we didn't define + follow-the-trail with brakes + explore budget; Parts 18/37/10). 4-5 = the per-category Market-State brief in Markdown (extends A's `report.py`, per the design target), HTML dashboard follow-up.

Decisions locked this session (do NOT relitigate without reason):
- **Output goal = a human "GPU Market Brief"** (deterministic Markdown, no LLM in the renderer; BLUF; demand|supply|gap as words+direction; NOW-vs-NEXT divergence; lead with "what moved"; cited + honest). Committed design target drives 4-2→4-5.
- **Lane discipline (Part 21 "counted once"; the 5-layer cake):** `merchant-gpu` owns only the merchant GPU vendors (NVIDIA/AMD/Intel) — momentum, unit economics, competitive position within merchant GPUs, product lead-times/pricing, strategic risk. Deep supply constraints (CoWoS/HBM/wafer/networking/power) and broad demand drivers (hyperscaler capex/inference/buildouts/ASIC threat) are **owned by sibling/adjacent agents** and reach merchant-gpu only as cited adjacent context. **4-2 does NOT annex them.**
- **The cross-cutting "GPU market state" brief is a LAYER-TIER product** (chips-layer agent reconciling foundry-packaging + hbm-memory + hyperscaler-asic + networking-silicon + merchant-gpu, reading energy/infra/models) — the named **future arc** after sp4's per-category mechanics, via the deferred Layer/Main tiers (Part 38).
- **Discovery of UNDEFINED topics + bounded rabbit-holing → 4-4** (the discovery lane: provisional threads, confidence-capped, promoted on persist+corroborate; follow-the-trail with brakes; explore budget). Substrate exists (4-1 provisional threads + the Part-37 gather). Not in 4-2.
- Full sequence: **4-1 (done) → 4-2 (spec written) → 4-3 → 4-4 → 4-5**, each merged to `main` before the next.

Honor the invariants (HANDOFF "OPERATING NOTES / INVARIANTS"): run from repo root with `.venv/Scripts/python`; **Claude Code is the brain** (no token/SDK/`[llm]`/API); **truly frozen — byte-unchanged:** `gate.py`, `scoring.py` (incl. `dmi_smi_contribution`), `registry/indicators.py` + `validate.py`, the `Finding` schema, the 6 dimension names, the rating scale, `pipeline.py` Part-7 gate, the existing `JsonStore`; **additive only (Part 33):** the `Scorecard` model, **registry DATA** (`indicators.json` — incl. the top-level `cadenceHorizon`/`sourceInventory` maps), the manifest, `report.py`, the skills, the judgment prompt/result, and NEW modules. **C-3 lesson:** per-indicator metadata is a TOP-LEVEL map keyed by id, never inside an indicator dict / never a new `IndicatorSpec` field. Doctrine: code computes+gates+stores; the agent never sets a number uncomputed (Part 17); fetched text is DATA not instructions (Part 8/26); caps/skips/gaps logged never silent (Part 29); paywalled inventoried+labeled `estimate`, never scraped (Part 22); replayable from the log + gated findings (Part 20). Full suite stays green after every task (baseline **248 passed, 3 skipped**); every commit ends with `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.

Bigger picture (don't lose it): the north star is a **daily, explainable GPU-market intelligence monitor running entirely inside Claude Code, one interface, Claude Code as the brain.** sp4 makes the per-category agent a daily monitor (Momentum vs Outlook, what-moved, threads, discovery). The *real* "understand the whole GPU market" product is the **layer-tier synthesis** that comes after. `main` is fully pushed (`origin/main` == local `main`); going forward, don't push unless asked.
